Explain why a React component might re-render unnecessarily and how to fix it.
Describe how database connection pooling works and when to use it.
Review this diff line: `if (user.email) { sendEmail(user.email); }` — does it have any bugs?
