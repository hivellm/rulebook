# Tasks: F2 — `.claude/rules/` generation

## 1. Templates (8 languages)
- [x] 1.1 `templates/rules/typescript.md` with `paths: ["**/*.ts", "**/*.tsx", "tsconfig*.json"]`
- [x] 1.2 `templates/rules/javascript.md`
- [x] 1.3 `templates/rules/rust.md` with `paths: ["**/*.rs", "Cargo.toml", "Cargo.lock", "rust-toolchain.toml"]`
- [x] 1.4 `templates/rules/python.md`
- [x] 1.5 `templates/rules/go.md`
- [x] 1.6 `templates/rules/cpp.md` (covers C and C++)
- [x] 1.7 `templates/rules/java.md`
- [x] 1.8 `templates/rules/csharp.md`

All templates begin with YAML frontmatter (`paths:`) followed by the canonical `Generated by @hivehub/rulebook v5.3.0` HTML-comment sentinel. Deleting the sentinel opts a user into "own this file" mode — rulebook will never regenerate it on update.

## 2. Generator
- [x] 2.1 Created `src/core/rules-generator.ts` with:
  - `generateRules(projectRoot, detection)` — emits a rule file per detected supported language
  - `resolveRuleLanguage(detected)` — maps aliases (`c` → `cpp`, `c#` → `csharp`, `ts` → `typescript`, etc.)
  - `hasGeneratedSentinel(content)` — detects whether a file is rulebook-generated
  - `listRulesWithSource(projectRoot)` — lists every rule file with `source: "generated" | "user"` and its `paths:` array
  - Constants: `SUPPORTED_RULE_LANGUAGES`, `GENERATED_SENTINEL`, `RULES_DIR`
- [x] 2.2 Skips files lacking the sentinel (preserved verbatim)
- [x] 2.3 Wired into `src/cli/commands.ts` for both `init` (after `mergeClaudeMd`) and `update` (after CLAUDE.md refresh, before the canonical rule-engine pass)

## 3. MCP
- [x] 3.1 Extended `rulebook_rules_list` in `src/mcp/rulebook-server.ts` to return `{ canonical, languageRules, count }` where `languageRules` entries carry `{ name, path, source, paths }`. Canonical rules (from `rule-engine.ts`) are unchanged.

## 4. Tail (mandatory)
- [x] 4.1 Documentation: inline comments on every exported symbol explain the contract; `proposal.md` references source
- [x] 4.2 Tests: `tests/rules-generator.test.ts` — 19 tests covering language resolution, template reading, sentinel detection, generation, user-file preservation, overwriting of stale generated files, idempotency, and listing with source classification
- [x] 4.3 Full suite: **1708 passed, 0 failed, 217 skipped**. Lint clean. Type-check clean.
